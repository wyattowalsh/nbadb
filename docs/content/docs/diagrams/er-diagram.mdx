---
title: ER Diagram
description: Whiteboard view of the nbadb star schema
---

import { Callout } from "fumadocs-ui/components/callout";

# Entity-Relationship Diagram

This curated page is the whiteboard version of the nbadb star surface: enough structure to see the shape of the warehouse quickly, without losing the grains that matter in analysis.

> **Whiteboard cue:** Start at `dim_game`, `dim_player`, and `dim_team`, then follow the spokes outward to event-level facts, rollups, and bridges.

<Callout type="info">
  Need the exhaustive, source-derived board? Use the companion [Auto ER
  Diagram](/docs/diagrams/er-auto), which is generated from schema definitions
  and should not be hand-edited.
</Callout>

## Quick navigation

<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">
  <ScoutCard title="Find the central spine" label="Entry surface">
    Start with <a href="#how-to-read-the-board">How to read the board</a> if you
    need the fastest route through `dim_game`, `dim_player`, and `dim_team`.
  </ScoutCard>
  <ScoutCard title="Read by warehouse role" label="Entry surface">
    Jump to <a href="#schema-categories">Schema categories</a> when you care
    more about table class counts than individual relationships.
  </ScoutCard>
  <ScoutCard
    title="Open the full generator-owned board"
    label="Generated companion"
  >
    Use <a href="/docs/diagrams/er-auto">Auto ER Diagram</a> when you need the
    exhaustive schema inventory rather than curated emphasis.
  </ScoutCard>
  <ScoutCard title="Leave the whiteboard for SQL planning" label="Next route">
    Skip to <a href="#next-steps-from-er-diagram">Next steps</a> when the
    picture is clear and the next move is relationships, schema reference, or
    lineage.
  </ScoutCard>
</div>

## Use this page when…

| If you need to answer…                                              | Start here                                      |
| ------------------------------------------------------------------- | ----------------------------------------------- |
| “Which dimensions anchor most joins?”                               | [How to read the board](#how-to-read-the-board) |
| “Which relationships are many-to-many or history-aware?”            | [How to read the board](#how-to-read-the-board) |
| “How many dimensions, facts, bridges, aggregates, and views exist?” | [Schema categories](#schema-categories)         |
| “Where is the full schema-derived roster of entities?”              | [Auto ER Diagram](/docs/diagrams/er-auto)       |

The nbadb star schema consists of **17 dimensions**, **72 fact tables**, and **2 bridge tables**.

```mermaid
erDiagram
    dim_player {
        int player_id PK
        string first_name
        string last_name
        string position
        string height
        string weight
        date birthdate
        string country
        string school
        int draft_year
        int draft_round
        int draft_number
        boolean is_active
    }

    dim_team {
        int team_id PK
        string abbreviation
        string nickname
        string city
        string full_name
        string conference
        string division
        string arena
        int year_founded
    }

    dim_game {
        string game_id PK
        date game_date
        int season_year
        string season_type
        int home_team_id FK
        int away_team_id FK
        int home_score
        int away_score
        string arena_name
    }

    dim_season {
        string season_id PK
        int year_start
        int year_end
        string season_type
        date start_date
        date end_date
    }

    dim_date {
        date date_id PK
        int year
        int month
        int day
        int day_of_week
        string month_name
        boolean is_weekend
    }

    dim_official {
        int official_id PK
        string first_name
        string last_name
        string jersey_num
    }

    dim_coach {
        int coach_id PK
        string first_name
        string last_name
        string coach_type
        boolean is_assistant
    }

    dim_arena {
        int arena_id PK
        string arena_name
        string arena_city
        string arena_state
        int capacity
    }

    dim_draft {
        int draft_id PK
        int season
        int round_number
        int round_pick
        int overall_pick
        string draft_type
    }

    dim_action_type {
        int action_type_id PK
        string action_type
        string sub_type
        boolean is_field_goal
    }

    dim_shot_zone {
        int zone_id PK
        string zone_basic
        string zone_area
        string zone_range
    }

    dim_play_type {
        int play_type_id PK
        string play_type
        string type_grouping
    }

    dim_lineup {
        int lineup_id PK
        string lineup_hash
        int team_id FK
    }

    fact_box_score_player ||--o{ dim_player : "player_id"
    fact_box_score_player ||--o{ dim_team : "team_id"
    fact_box_score_player ||--o{ dim_game : "game_id"

    fact_box_score_team ||--o{ dim_team : "team_id"
    fact_box_score_team ||--o{ dim_game : "game_id"

    fact_play_by_play ||--o{ dim_game : "game_id"
    fact_play_by_play ||--o{ dim_player : "person_id"
    fact_play_by_play ||--o{ dim_team : "team_id"
    fact_play_by_play ||--o{ dim_action_type : "action_type_id"

    fact_shot_chart ||--o{ dim_game : "game_id"
    fact_shot_chart ||--o{ dim_player : "player_id"
    fact_shot_chart ||--o{ dim_team : "team_id"
    fact_shot_chart ||--o{ dim_shot_zone : "zone_id"

    fact_game_log ||--o{ dim_game : "game_id"
    fact_game_log ||--o{ dim_team : "team_id"
    fact_game_log ||--o{ dim_season : "season_id"

    fact_player_game_log ||--o{ dim_game : "game_id"
    fact_player_game_log ||--o{ dim_player : "player_id"
    fact_player_game_log ||--o{ dim_season : "season_id"

    fact_standings ||--o{ dim_team : "team_id"
    fact_standings ||--o{ dim_season : "season_id"

    fact_matchup ||--o{ dim_game : "game_id"
    fact_matchup ||--o{ dim_player : "off_player_id"
    fact_matchup ||--o{ dim_player : "def_player_id"

    fact_rotation ||--o{ dim_game : "game_id"
    fact_rotation ||--o{ dim_player : "person_id"
    fact_rotation ||--o{ dim_team : "team_id"

    fact_win_probability ||--o{ dim_game : "game_id"
    fact_win_probability ||--o{ dim_team : "home_team_id"

    fact_draft ||--o{ dim_player : "person_id"
    fact_draft ||--o{ dim_team : "team_id"
    fact_draft ||--o{ dim_draft : "draft_id"

    fact_synergy ||--o{ dim_player : "player_id"
    fact_synergy ||--o{ dim_team : "team_id"
    fact_synergy ||--o{ dim_play_type : "play_type_id"

    bridge_game_official ||--o{ dim_game : "game_id"
    bridge_game_official ||--o{ dim_official : "official_id"

    bridge_play_player ||--o{ dim_game : "game_id"
    bridge_play_player ||--o{ dim_player : "player_id"

    fact_box_score_player {
        string game_id FK
        int player_id FK
        int team_id FK
        float min
        float pts
        float reb
        float ast
        float stl
        float blk
        float fgm
        float fga
        float fg3m
        float fg3a
        float ftm
        float fta
        float plus_minus
    }

    fact_box_score_team {
        string game_id FK
        int team_id FK
        float pts
        float reb
        float ast
        float fgm
        float fga
        float fg_pct
    }
```

## How to Read the Board

If the board feels dense on first glance, read it in three laps: start at the conformed spine, fan
out into fact neighborhoods, then finish with bridges and history-aware dimensions.

### Scan order

1. **Center first** — Find `dim_game`, `dim_player`, and `dim_team` before you read any peripheral table.
2. **Follow the repeated grains** — Look for player-game, team-game, play-by-play, and shot-level fact clusters.
3. **Finish on exceptions** — Bridge tables and SCD2 dimensions explain the relationships that do not fit a single-grain path.

| Scan first                           | What to trace                                                              | Why it matters                                                                                                |
| ------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `dim_game`, `dim_player`, `dim_team` | The busiest join paths                                                     | These conformed dimensions sit at the center of most analytical queries                                       |
| Fact clusters                        | Repeated grains such as player-game, team-game, tracking, and play-by-play | They show how nbadb separates possessions, summaries, and specialty surfaces without collapsing them together |
| Bridge tables                        | `bridge_game_official` and `bridge_play_player`                            | They capture many-to-many relationships that do not belong to a single fact grain                             |
| History-aware dimensions             | `dim_player` and `dim_team_history`                                        | They preserve roster and franchise changes with SCD2 fields instead of overwriting the past                   |

## Schema Categories

| Category   | Count | Description                    |
| ---------- | ----- | ------------------------------ |
| Dimensions | 17    | Slowly changing reference data |
| Facts      | 72    | Event-level transactional data |
| Bridges    | 2     | Many-to-many associations      |
| Derived    | 16    | Pre-aggregated rollups         |
| Views      | 4     | Denormalized analytics views   |

<CourtDivider label="Next board cut" />

## Next steps from ER diagram

<div className="grid gap-4 md:grid-cols-3">
  <ScoutCard title="Open the generator-owned full roster" label="Next stop">
    Jump to <a href="/docs/diagrams/er-auto">Auto ER Diagram</a> when you need
    the exhaustive, schema-derived inventory rather than the curated whiteboard
    view.
  </ScoutCard>
  <ScoutCard title="Turn structure into join plans" label="Next stop">
    Continue to <a href="/docs/schema/relationships">Relationships</a> or{" "}
    <a href="/docs/schema">Schema Reference</a> when the next question is how to
    join this surface safely in SQL.
  </ScoutCard>
  <ScoutCard title="Trace one table back to the inbound feed" label="Next stop">
    Move to <a href="/docs/lineage/table-lineage">Table Lineage</a> when the
    diagram has shown where a table sits, but you still need to know which
    source endpoints and staging tables feed it.
  </ScoutCard>
</div>
